# Base URL para API backend
# Si usas .NET en Visual Studio, típicamente es uno de estos:
#  - HTTPS: https://localhost:5001
#  - HTTP:  http://localhost:5000
VITE_API_BASE_URL=https://localhost:5001

# Flags de features (opcional)
VITE_FEATURE_SKILLS=true

# Requerir autenticación para acceder a rutas protegidas
# true (por defecto): exige token y redirige a /login
# false: permite acceso como invitado y oculta la pantalla /login
VITE_REQUIRE_AUTH=true

# Usar proxy local en desarrollo para evitar CORS
# Si es true, axios usará baseURL "/api" y el dev server redirigirá a VITE_PROXY_TARGET
VITE_USE_PROXY=false
# Destino del proxy (tu backend local)
VITE_PROXY_TARGET=https://localhost:5001
